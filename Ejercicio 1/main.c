#include <stdio.h> //directivas de pre-procesador | standart input output, libreria de entradas y salidas estandar
#include <stdlib.h> //directivas de pre-procesador |

//Si queres que te tome las comillas, tenes que poner una barra invertida y depués las comillas

// sizeof = "tamaño de" (te voy a pasar algo y vos decime cuanto ocupa en memoria)

int main()
{
    printf("%d  \n\n", sizeof(int)); //le ponemos una mascara
    system(("pause"));

    return 0;
}
